{
	"name": "nicegui",
	"displayName": "NiceGUI",
	"icon": "icon.png",
	"version": "0.10.0",
	"description": "Language support for the NiceGUI framework",
	"repository": {
		"type": "git",
		"url": "https://github.com/daelonsuzuka/nicegui-vscode"
	},
	"bugs": {
		"url": "https://github.com/daelonsuzuka/nicegui-vscode/issues"
	},
	"license": "MIT",
	"author": "daelonsuzuka",
	"publisher": "daelonsuzuka",
	"engines": {
		"vscode": "^1.109.0"
	},
	"categories": ["Programming Languages", "Snippets", "Other"],
	"activationEvents": ["onLanguage:python"],
	"scripts": {
		"compile": "tsc -p ./",
		"lint": "eslint ./src --quiet",
		"watch": "tsc -watch -p ./",
		"package": "vsce package",
		"sync:nicegui-data": "python3 tools/gather_nicegui_data.py",
		"sync:quasar-data": "python3 tools/gather_props.py",
		"vscode:prepublish": "npm run esbuild-base -- --minify",
		"esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
		"esbuild": "npm run esbuild-base -- --sourcemap",
		"esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
		"test": "vscode-test"
	},
	"main": "./out/extension.js",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "NiceGUI",
			"properties": {
				"nicegui.enableStringSuggestions": {
					"type": "boolean",
					"default": true,
					"description": "Whether to automatically 'Quick Suggestions' in strings."
				},
				"nicegui.preview.url": {
					"type": "string",
					"default": "http://localhost:8080",
					"description": "The target URL for the NiceGUI preview"
				},
				"nicegui.performance.maxGeneralCompletions": {
					"type": "number",
					"default": 300,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of general completion items returned per request."
				},
				"nicegui.performance.maxIconCompletions": {
					"type": "number",
					"default": 300,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of icon completion items returned per request."
				},
				"nicegui.performance.maxTailwindCompletions": {
					"type": "number",
					"default": 300,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of tailwind class completion items returned per request."
				},
				"nicegui.performance.maxIconValueCompletions": {
					"type": "number",
					"default": 200,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of icon value suggestions returned for props like icon=."
				},
				"nicegui.performance.maxAttributeValueCompletions": {
					"type": "number",
					"default": 200,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of attribute value completion items returned per request."
				},
				"nicegui.performance.maxFunctionCompletions": {
					"type": "number",
					"default": 200,
					"minimum": 1,
					"maximum": 2000,
					"description": "Maximum number of ui.* function completion items returned per request."
				},
				"nicegui.performance.enableCompletionTimingLog": {
					"type": "boolean",
					"default": false,
					"description": "Enable timing logs for completion requests in the Extension Host log."
				},
				"nicegui.performance.completionTimingLogThresholdMs": {
					"type": "number",
					"default": 20,
					"minimum": 1,
					"maximum": 10000,
					"description": "Only log completion timing when duration is at least this many milliseconds."
				}
			}
		},
		"commands": [
			{
				"category": "NiceGUI",
				"command": "nicegui.openPreview",
				"title": "Open NiceGUI Preview"
			},
			{
				"category": "NiceGUI",
				"command": "nicegui.switchScriptComponent",
				"title": "Switch Script/Component"
			}
		],
		"keybindings": [
			{
				"command": "nicegui.switchScriptComponent",
				"key": "alt+o",
				"when": "editorLangId in nicegui.context.niceguiComponentFiles && editorTextFocus"
			}
		],
		"grammars": [
			{
				"injectTo": ["source.python"],
				"scopeName": "source.nicegui.injections",
				"path": "./syntaxes/nicegui.tmLanguage.json",
				"embeddedLanguages": {
					"meta.embedded.inline.html": "html",
					"meta.embedded.inline.css": "css",
					"meta.embedded.inline.scss": "scss",
					"meta.embedded.inline.sass": "sass"
				}
			}
		],
		"snippets": [
			{
				"language": "python",
				"path": "./configurations/snippets.json"
			}
		]
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.14",
		"@types/node": "^22.19.10",
		"@types/vscode": "^1.109.0",
		"@types/ws": "^8.18.1",
		"@vscode/vsce": "^3.7.1",
		"esbuild": "^0.27.3",
		"glob": "^13.0.1",
		"ts-node": "^10.9.2",
		"tslint": "^6.1.3",
		"typescript": "^5.6.2"
	}
}
